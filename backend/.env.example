# MongoDB (for legacy routes)
MONGODB_URI=mongodb://localhost:27017/churn-prediction

# JWT Configuration
JWT_SECRET=your_super_secret_key_change_this_in_production_min_32_chars
JWT_EXPIRES_IN=24h

# Server
PORT=5000
NODE_ENV=development

# Database (SQLite is file-based, no config needed)
# SQLite database will be created as database.db in the backend root
